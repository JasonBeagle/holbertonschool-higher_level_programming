#!/usr/bin/python3
"""task 2-is_same_class
    which returns true if object is exactly
    an instanced, or false otherwise."""


def is_same_class(obj, a_class):
    """determines if an object is
        the same as the instanced class"""
    pass

    return True if type(obj) is a_class else False

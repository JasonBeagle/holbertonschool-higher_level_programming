#!/usr/bin/python3
"""task 3-is_kind_of_class
    returns True if an object is an instance of
    parent or child class otherwise returns False"""


def is_kind_of_class(obj, a_class):
    """returns true if instance is same
        as parent or child, returns false otherwise"""

    pass

    return isinstance(obj, a_class)